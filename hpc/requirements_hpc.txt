# HPC requirements for ATOMs baseline computation on MPCDF Viper-CPU.
# Tested on Python 3.13 (python-waterboa/2025.06).
#
# Install with:
#   pip install -r hpc/requirements_hpc.txt
#
# PyTorch CPU wheels are fetched from the PyTorch index (see --extra-index-url).
# torch 2.5.0 is the earliest build available for Python 3.13.

--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML — torch 2.6.0 is the first stable release with full Py3.13 support.
# torchvision version must match torch exactly.
torch==2.6.0+cpu
torchvision==0.21.0+cpu
timm>=1.0.0

# LRP
zennit

# TFV6 dependencies
beartype>=0.18.5
opencv-python-headless>=4.10.0

# ATOMs / data processing
# numpy 2.x is required for Python 3.13 (1.24 does not support it)
numpy>=2.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.4.0

# Plotting (imported at module level by visualization_carla.py)
matplotlib>=3.8.0

# Config / misc
pyyaml>=6.0
