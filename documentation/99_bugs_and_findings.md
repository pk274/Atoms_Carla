# Bug & Findings Log

Running log from the documentation sweep. Nothing has been changed in code — flagged only. Severity: low / med / high.

## From Topic 1 — Architecture & Config

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 1.1 | `atoms_config.py:84` vs `hpc/prep_test.py:42,55` | med | `PGD_EPSILON = 14.0` claims to match HPC default, but HPC default is **12.0** — recorded PGD intensity and the attack actually crafted can disagree | Single source of truth; pass `--pgd-epsilon` explicitly |
| 1.2 | `run_analysis.py:11`, `run_online_analysis.py:11` | low | Docstrings reference nonexistent `conf.MODEL_PATH`; model paths hardcoded at `run_analysis.py:134/:172` | Move paths into config or fix docstrings |
| 1.3 | `atoms_config.py:33,76` | low | `AFFECT_BOTH_CAMS`, `ADD_AUTOPILOT_VEHICLES` consumed nowhere | Delete or wire up |
| 1.4 | `CLAUDE.md` (ATOMs section) | med | Claims combinatorial attention c(T) (α=0.25) is implemented in `atoms_carla.py` — no such code exists; only hierarchical attention is implemented | Correct CLAUDE.md; Topic 4 to confirm |
| 1.5 | `sweep_clusters.py:33 vs :42` | low | `K_VALUES_DEFAULT = [18,20,22]` but help text says "2 4 6 8 … 20" | Sync help string — **superseded by 8.7**: the literal has since changed to `[10, 8, 12]`; the help-text mismatch persists against the new literal |
| 1.6 | `atoms_config.py:127` | low | `if ... and False:` permanently disables alternative Town05 spawn — dead code | Remove or make real flag |
| 1.7 | `atoms_config.py:87` + `run_analysis.py:201` | low/med | `DEFAULT_CMD = 2` but comment says "3 = FOLLOW_LANE"; under 0-based mapping 2 = STRAIGHT. Affects `_make_minimal_data` conditioning when cmd missing | Clarify intended default, align |
| 1.8 | `CLAUDE.md` Stage 1 | low/med | `unzip_routes.ps1` documented but absent from repo — dataset selection not reproducible from version control | Commit the script |
| 1.9 | `perturbation_manager.py:78` | low | Comment "recompute attack every 3rd frame" but `attack_interval = FRAMES_TO_SKIP+1 = 1` | Stale comment |
| 1.10 | `atoms_config.py:21 vs :89` | low | `NUM_GMM_CLUSTERS = 12` > `GMM_MAX_K = 10` — forced K outside BIC/AIC sweep range, no consistency check | Raise `GMM_MAX_K` or assert — **superseded by 7.1**: `NUM_GMM_CLUSTERS` is now `10` (= `GMM_MAX_K`), so the out-of-range condition is resolved; the residual issue is that the forced K still always overrides the BIC/AIC sweep |
| 1.11 | `run_online_analysis.py:1–31` | low | Module docstring is a verbatim copy of `run_analysis.py`'s | Rewrite header |
| 1.12 | `run_analysis.py:~190` | low | Comment "raw 23-class CARLA scheme"; `CARLA_CLASSES` has 29 entries | Update comment |
| 1.13 | `atoms_config.py:9` + `run_analysis.py:130` | low | Config docstring accepts `AGENT="LBC"` but it silently loads the WoR model | Remove LBC or raise explicit error |
| 1.14 | `atoms_config.py:88` + `run_analysis.py:431`, `detectors.py:163` | low | `MAHAL_RIDGE = 0.01` vs stale comment "1e-6 default; increase to 1e-4" | Update comment |

## From Topic 2 — Agents & F_c

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 2.1 | `CLAUDE.md` (WoR section) vs `pcla_agents/wor/image_agent.py:119-126,147,182` | med | CLAUDE.md states WoR inputs "wide RGB (160×704) + narrow RGB (88×352)"; actual model inputs are wide [3,192,480] (3×160×240 cams, top-48 crop) and narrow [3,160,384] (384×240, bottom-80 crop). Verified against `data/WOR/baseline_data/frames/*.npz` | Correct CLAUDE.md |
| 2.2 | Training (6-cam, 2304 px) vs analysis frames (3-cam, 1152 px); `transfuser_backbone.py:36`, `migrate_lead_to_baseline.py:38`, `data/TFV6/*/frames/*.npz` | med/high | `visiononly_resnet34` was trained on 6-camera 2304-wide images (`config.json` final_image_width=2304); all offline LEAD frames are 3-camera 1152-wide. `AdaptiveAvgPool2d((12,72))` silently upsamples the 12×36 stage-4 map to 12×72, hiding the mismatch; camera geometry also differs (yaw ±54.5° vs ±57.5°). Live-pert data (2304) and LEAD baselines (1152) differ from each other too | Acknowledge as systematic domain shift in the thesis; or re-extract frames with a 6-cam-compatible source / use a 3-cam checkpoint |
| 2.3 | `CLAUDE.md` Stage 1, `ATOMs_Analysis/saliency/lrp_transfuser.py:7,943` | low/med | "concatenated 6-camera JPEG (384×1152 px)" — it is 3 cameras × 384 px (`migrate_lead_to_baseline.py:38`). lrp_transfuser docstrings claim [B,3,384,2304] / "W'=72 for 384×2304 input", which holds only for live-agent input, not the analyzed frames (W'=36 pre-pool) | Fix CLAUDE.md camera count; qualify docstrings |
| 2.4 | `lrp_transfuser.py:948` vs `config_training.py:633-645` | low | `_SPEED_BINS_PROXY = [.., 13.89, .., 17.78, 20.]` is a hardcoded rounded duplicate of `target_speed_classes` ([.., 13.88888888, .., 17.77777777, ..]) used to decode MDX-v2 throttle proxy | Read bins from `full_model.config.target_speed_classes` |
| 2.5 | `run_analysis.py:141-145`, `hpc/compute_*_chunk.py` vs `open_loop_inference.py:54-81` | med | Live agent drives with a 3-model ensemble (model_0030_0/1/2.pth, predictions averaged); analysis loads only `sorted(...)[0]` = model_0030_0.pth. LRP/ATOMs explain a different (single-member) policy than the one that produced live recordings | Either analyze all members, or state single-member analysis explicitly in the thesis |
| 2.6 | `docs/design_decisions.md:114` | low | "LRP1 seed: One-hot at the argmax of target_speed_decoder(...)" is stale — superseded by Decision D (softmax distribution seed, `docs/lrp_todo.md:284-294`, implemented `lrp_transfuser.py:771`) | Update design_decisions.md |
| 2.7 | `data/WOR/baseline_data/frames/*.npz` vs `image_agent.py:182-183` | low | Stored `seg_red_narr` is (N,192,384) while `narr_rgb` is (N,160,384) — narrow seg mask is 32 rows taller than the RGB it should align with (current code crops both to 160; data predates the fix or used a different crop). Harmless while `WIDE_ONLY_PROFILE=True`, but would misalign ATOMs masks if narrow profiles are ever enabled | Re-record or crop seg_red_narr on load; add shape assert in ATOMsCarla |
| 2.8 | `pcla_agents/transfuserv6/lead/inference/sensor_agent.py:233` | med | `carla.VehicleLightState(carla.VehicleLightState.Union[Position, carla].VehicleLightState.LowBeam)` — corrupted expression (broken refactor); crashes whenever night-mode weather is set via `random_weather`/`custom_weather` | Restore `carla.VehicleLightState(carla.VehicleLightState.Position | carla.VehicleLightState.LowBeam)` |
| 2.9 | `sensor_agent_data_collection.py:175-181` | low | `destroy()` flush is commented out — frames are saved only when the 100-frame buffer fills (`baseline_dataset.py:211-214`); the final partial buffer of each episode is silently discarded | Re-enable flush or document intentional drop |
| 2.10 | `config_training.py:57-63` | low | `num_available_cameras` dict literal contains the key `CARLA_LEADERBOARD2_3CAMERAS` twice (lines 58 and 60) — harmless but indicates copy-paste; same pattern as the tolerated "leaderboad" typo paths (`config_training.py:44-47`) | Remove duplicate key |
| 2.11 | `tfv6.py:124` | low | `bev_feature_grid = self.backbone.top_down(bev_features)` computed unconditionally even when `detect_boxes` and `use_bev_semantic` are both False (visiononly) — wasted compute every live inference step | Guard with the same predicates as its consumers |
| 2.12 | `lrp_transfuser.py:1040-1047` vs `hpc/compute_baseline_chunk.py:133`, `atoms_config.py:87` | low | Default command inconsistency around F_c conditioning: `_make_minimal_data` defaults `cmd=3` ("FOLLOW_LANE", correct in 0-based scheme), while ATOMs pipeline passes `default_cmd=2` with comment "(FOLLOW_LANE)" — 2 is STRAIGHT. Extends finding 1.7 | Align on 3 = LANEFOLLOW everywhere |

## From Topic 3 — LRP

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 3.1 | `docs/design_decisions.md:165,172-187` vs `lrp_transfuser.py:658,666` | med | design_decisions.md is stale in two places: §"Composite rule split" and §"zennit composite — no canonizer" still document `Epsilon(ε=1e-6)` (actual: 1e-2, changed 2026-05-28 after Bug C) and claim "no canonizer / BatchNorm not merged" (actual: `SequentialMergeBatchNorm` canonizer active since Issue-7 fix). Extends 2.6 (stale argmax-seed claim at line 114) | Rewrite both sections to match current code |
| 3.2 | `CLAUDE.md` ("see docs/lrp_todo.md for ... remaining open issues (BatchNorm canonization, contrastive seeding)") vs `docs/lrp_todo.md:225,352,356-383` | low | CLAUDE.md presents BatchNorm canonization and contrastive seeding as open issues, but lrp_todo marks both resolved ("Remaining: Nothing blocking") and code confirms (canonizer at `lrp_transfuser.py:666`; `forced_brake/forced_drive` implemented). Also lrp_todo Issue 7 said a *custom* canonizer was needed ("timm differs from torchvision, ResNetCanonizer cannot be used"); the actual fix uses zennit's generic order-based `SequentialMergeBatchNorm`, no custom code | Update CLAUDE.md sentence; add one line to lrp_todo Issue 7 noting the generic canonizer sufficed |
| 3.3 | `lrp_analysis.py:10-11,27,190` + `pcla_agents/wor/common/resnet.py:41` | med | WoR docstring claims "ResNetCanonizer handles BasicBlock skip connections and merges BatchNorm". Verified against zennit 0.5.1: `ResNetBasicBlockCanonizer`/`ResNetBottleneckCanonizer` match `isinstance(module, torchvision.models.resnet.BasicBlock/Bottleneck)`; WoR uses a local copy of torchvision ResNet with its own `BasicBlock` class, so the skip-connection canonization silently no-ops. Only the `SequentialMergeBatchNorm` component is effective (BN merge works). Residual adds are handled by plain autograd — same accepted simplification as TFV6, but the docstring claim is wrong | Correct docstring; optionally adapt the block canonizers to the local BasicBlock type |
| 3.4 | `lrp_analysis.py:186-191` (`first_map`) + zennit `SpecialFirstLayerMapComposite.mapping` (verified 0.5.1) | low/med | The WSquare input-layer rule is assigned only to the FIRST matching Convolution in module-traversal order — in `JointCameraForLRP` that is the wide backbone's stem conv. The narrow backbone's stem conv receives AlphaBeta(1,0) instead, i.e. the two cameras get inconsistent input-layer treatment. (Not an issue for TFV6: single trainable input stream.) | Use a NameMapComposite entry (or second first-map mechanism) to give both stem convs WSquare |
| 3.5 | `lrp_analysis.py:592-631` (`attribute_action`) | low | Broken dead code: calls `forward_relevance(wide_rgb, beg="output", end="input", cmd=cmd)` without the required positional `narr_rgb` (TypeError) and without `spd` (the mode raises ValueError); the computed `action_idx` is never used; `bin_idx`/`action_type` therefore have no effect. No callers in the repo | Delete, or rewrite to build a one-logit selector mask and pass narr_rgb/spd |
| 3.6 | `lrp_transfuser.py:259-264` + `LRPSoftmax.backward` (77-80) | low | Latent NaN: if `attn_mask`/`key_padding_mask` were ever passed, masked scores are `-inf`; backward computes `x * (R - s*ΣR)` = `-inf * 0` = NaN. Currently safe — the planning decoder is invoked without masks (`lrp_transfuser.py:534-537`) | Guard: `torch.where(torch.isfinite(x), x*(R-s*R_sum), 0.)` in `LRPSoftmax.backward` |
| 3.7 | `lrp_transfuser.py:184,257` | low | The attention scale `d_head^-0.5` is applied as a plain multiplication *outside* `LRPMatMul`, so the backward multiplies relevance by `1/√d` (gradient semantics) instead of treating multiplication-by-constant as relevance-identity. The q/k-branch relevance is systematically attenuated relative to the v-branch in every attention layer. Cancels under ATOMs per-frame normalization, but contributes to the non-conservation budget (D06) | Fold the scale into `LRPMatMul` (divide q before apply, or apply identity backward for the constant) |
| 3.8 | `lrp_transfuser.py:295-298` (`TransformerDecoderLayerExplicit`) | low | Hardcodes `nn.GELU()` and post-norm; `from_module` neither copies `layer.activation` nor checks `layer.norm_first`. Currently correct (matches `planning_decoder.py:47-56`: GELU, default `norm_first=False`) but silently diverges if the training config changes. Activation is acknowledged in a comment; norm_first is not | Assert `not layer.norm_first` and copy/compare the activation in `from_module` |
| 3.9 | `lrp_transfuser.py:390-399` + `SequentialMergeBatchNorm` semantics | low | `TFv6FullModelForLRP` shares `image_encoder`/`lidar_encoder`/avgpools/channel-projections by reference with the live `model.backbone` (only transformers + planning decoder are deep-copied). The BN canonizer temporarily mutates those shared conv weights in-place inside every composite context (restored on exit). Safe in the current single-threaded pipeline, but the original backbone is silently modified during attribution and any concurrent forward (or exception before context exit) would see fused weights | Deep-copy the encoders too, or document the invariant at the constructor |
| 3.10 | `environment.yml` | low/med | `zennit` is not listed in `environment.yml` (checked: no match), although it is the core dependency of both LRP backends; the installed version determines canonizer/composite semantics relied upon by findings 3.3/3.4 | Pin `zennit==0.5.x` in the pip section |

## From Topic 4 — ATOMs

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 4.1 | `CLAUDE.md` (ATOMs section) vs `atoms_carla.py` (full file) | med | **Confirms 1.4 definitively**: combinatorial attention c(T) (α=0.25) is not implemented anywhere — `atoms_carla.py` computes only `_hierarchical`; repo-wide grep for combinatorial/α-threshold logic matches nothing (only `viz_config.py:206`, an unrelated plot alpha). Profiles are hierarchical-only | Correct CLAUDE.md; in the thesis, call the feature "hierarchical ATOMs" |
| 4.2 | `atoms_carla.py:203-207` vs `:264-268` | low/med | Docstring advertises `mode_analysis=4` ("layer-output: output→input, single map"), but the dispatch dict holds only {1,2,3} and `.get(mode, self._compute_node_level)` makes mode 4 (or any unknown value) silently run mode 1 (the most expensive mode) | Implement mode 4 or remove it from the docstring; raise on unknown modes |
| 4.3 | `hpc/compute_baseline_chunk.py:132`, `hpc/compute_test_chunk.py:153`, `hpc/compute_live_pert_chunk.py:141` vs `atoms_config.py:24`, `atoms_carla.py:241` | med/high | HPC chunk scripts hardcode `p_relevance=0.25` (with comment "FC_RELEVANCE_FILTER"), but config and class default are 0.9 (paper value). HPC `--mode-analysis` defaults to 1 (`array_task.sh:48`: `${MODE_ANALYSIS:-1}`), the only mode where p matters — so all HPC mode-1 profiles use a 25% mass filter and are not reproducible with local defaults | **FIXED 2026-06-14**: all three chunk scripts changed to `p_relevance=0.9` to match `conf.FC_RELEVANCE_FILTER`. ⚠ **Profiles computed before this date used 0.25** — recompute on HPC to obtain 0.9 profiles, or report 0.25 as the value actually used. Long-term: source from a shared config (finding 10.8) |
| 4.4 | `run_analysis.py:768` vs `baseline_dataset.py:518`, `hpc/compute_test_chunk.py:219` | med | Local test-profile loop calls `atoms.process_frame(..., cmd=cmd)` **without `spd`** → speed token forced to 0.0 for every test frame, while baseline profiles use the true speed. Systematic baseline/test conditioning asymmetry (TFV6) unrelated to perturbations; HPC test path and `run_online_analysis.py:438,510` pass spd correctly | Add `spd=float(test_data["speed"][i])` at `run_analysis.py:768` |
| 4.5 | `atoms_carla.py:435,629-658` + `lrp_transfuser.py:680-737` + `distance_computer.py:358-362` | med | TFV6 pixel maps fed to `_give_element_selectivity` are raw and **signed** (AttnLRP); `abs()` is applied only to node weights (mode 1), never to maps. Per-class sums and hence profile entries can be negative, and `process_frame:334-335` normalizes by a signed total. Downstream, Wasserstein raises on genuinely negative weights; JSD silently clips to 1e-12. WoR is safe (cross-normalize takes abs, `lrp_analysis.py:341-344`) | Decide policy (abs or pos-clip the map, or assert non-negativity) and document; at minimum guard `process_frame` against near-zero/negative totals |
| 4.6 | `atoms_carla.py:634-637` | low | `_give_element_selectivity` docstring claims "Both maps already sum to (wide_fraction) and (narr_fraction)" — true only for WoR cross-normalized maps; TFV6 maps arrive raw/unnormalized (`forward_relevance` returns raw rel with wide_frac=1.0). Harmless for results (per-frame renormalization) but misleading | Qualify docstring per agent |
| 4.7 | `atoms_carla.py:495,594,618-620` | low | `narr_r / (1 - norm_w)` divides by zero when `wide_frac == 1.0` and a narrow map exists (WoR edge case: zero narrow relevance) — produces inf in `saliency_data_narr_*` (visualization only) | Guard with `if norm_w < 1.0` |
| 4.8 | `baseline_dataset.py:543` vs `:548` | low/med | `rgb_wide` is assigned only inside the `if conf.PLOT_COMPARATIVE_REL:` branch but used unconditionally by `visualize_relevance` at `:548` → NameError on the first plotted frame when `PLOT_SEG_AND_REL=True` and `PLOT_COMPARATIVE_REL=False` | Hoist `rgb_wide = wide[0].permute(...)` above the comparative branch |
| 4.9 | `atoms_carla.py:57-103` vs `utils/visualization_carla.py:57-104` | low | `CARLA_CLASSES` and `TFV6_CLASSES` are defined twice (currently identical). Local pipelines import from `atoms_carla`, HPC chunk scripts from `visualization_carla` — silent divergence risk for profile semantics | Single definition; re-export from one module |
| 4.10 | `atoms_carla.py:18,106-107,209-210` + `run_analysis.py:196` | low | Stale internal docs: header says CARLA tags "0–22" (registry has 0–28); `use_reduced` docstrings say "7 driving-relevant classes" but `REDUCED_CLASS_IDS` has 8 entries; `run_analysis.py:196` repeats "all 23" | Update comments |
| 4.11 | `docs/design_decisions.md` (no ATOMs-metric section) | low | design_decisions.md documents F_c choice, cmd one-hot fix, and the shared-ATOMs-class note, but nowhere records that c(T) was dropped, that MODE_ANALYSIS=2 deviates from the paper (activation/ones seed, no node filter), or the abs-node-weight decision (`atoms_carla.py:431-435`) — all paper-deviating design choices per the documentation policy | Add an "ATOMs deviations from Beylier et al." section (content now in `documentation/04_atoms.md` §10) |

## From Topic 5 — Dataset Creation

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 5.1 | `ATOMs_Analysis/detection/dataset.py:543-544` vs `migrate_lead_to_baseline.py:172-173`, `baseline_dataset.py:96-98` | low | `dataset._to_hw_uint8` slices semantic channel **2** (blue) while the migration writer and `baseline_dataset._to_hw_uint8` use channel **0** — and `baseline_dataset.py:96-98` explicitly comments that channel 2 is only correct for BGRA and should be avoided. Identical result in practice (CARLA semantic R=G=B=class_id), but the two helpers disagree and the `dataset.py` copy contradicts the documented fix | Use channel 0 in `dataset._to_hw_uint8` to match; or share one helper |
| 5.2 | `migrate_lead_to_baseline.py:259-276, 421-422` | low/med | Canonical/test/val frame sampling is unseeded and deterministic only w.r.t. route-discovery order; `n_frames`/`testset_n_frames` are upper bounds, not exact counts (truncation `[:frames_per_town]`), and a large stride can skip entire short routes. No manifest of which frames were selected is written, so the realised dataset is not reproducible from the CLI args alone | Write a sampling manifest (route+frame_idx list) and/or seed the stride offset; document that targets are upper bounds |
| 5.3 | `docs/design_decisions.md:296,305-308` | low | Stale: claims RGB "expected 2304×384" and lists open `TODO_SHAPE`/`TODO_CMD`/`TODO_TOWN` "until a sample file is inspected" — all three are resolved in code (`migrate_lead_to_baseline.py:38,150` confirm (384,1152,3); command map and town key implemented). Relates to 2.2/2.3 (camera-count) | Update section: mark TODOs done, fix 2304→1152 |
| 5.4 | `CLAUDE.md` Stage 1 + `migrate_lead_to_baseline.py:259-276` | low/med | New angle on 1.8: dataset membership is doubly non-reproducible — `unzip_routes.ps1` (route selection) is absent **and** canonical-mode frame sampling is unseeded. Even with identical extracted routes, baseline/test/val frame sets cannot be regenerated deterministically (only `alt_split` is seeded via `RANDOM_SEED`) | Commit `unzip_routes.ps1` (1.8) and seed/manifest the migration sampling (5.2) |
| 5.5 | `migrate_lead_to_baseline.py:557-564` vs `atoms_config.py:60-74` | med | `migrate_alt_split` hardcodes output to `*_data_alt` dirs regardless of `EXPERIMENT_VARIANT`, while the analysis reads `_alt` paths only when `EXPERIMENT_VARIANT="alternative"`. Running `alt_split` with the flag left at `"original"` (or canonical migration with the flag at `"alternative"`) silently writes data that is never read. No guard couples the two | Assert `EXPERIMENT_VARIANT=="alternative"` in `migrate_alt_split` (or read its output dirs from config) |
| 5.6 | `migrate_lead_to_baseline.py:462-468` | low | Val auto-exclusion strips the `run_<town>_` prefix from test npz stems assuming canonical naming; it would silently fail to exclude `alt_split`-named files or any renamed scheme (no overlap-detected assertion), reintroducing test/val leakage undetected | Match on full stem set intersection, or assert naming convention |
| 5.7 | `CLAUDE.md` (Data Layout) + `atoms_config.py:63` | low | `CLAUDE.md`'s Data Layout and most invocation examples describe the canonical `baseline_data`/`test_data`/`val_data`/`results` paths, but the live config sets `EXPERIMENT_VARIANT="alternative"`, so all paths actually resolve to `*_data_alt`/`results_alt`. Reader following the docs inspects the wrong (possibly empty) directories | Note the currently-active variant in CLAUDE.md, or document both path sets explicitly |

## From Topic 6 — Perturbations

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 6.1 | `CLAUDE.md` ("Perturbation types" table) vs `perturbation_manager.py:732-1083` | low/med | CLAUDE.md lists only 4 perturbations; the registry has **9** (`gaussian_noise`, `brightness_scale`, `camera_loss`, `isolate_channel`, `mirror_horizontal`, `camera_swap`, `blur`, `salt_and_pepper`, `phantom_obstacle`) plus `pgd`/`fgsm` adversarial methods | List all registered perturbations; mark which feed the offline mix |
| 6.2 | `run_analysis.py:696-710`, `prep_test.py:35-42` vs `perturbation_manager.py` | low/med | Only `gaussian_noise`/`brightness_scale`/`camera_loss`/`pgd` are wired into the 5-way offline ROC/AUC mix. `isolate_channel`/`mirror_horizontal`/`camera_swap`/`blur`/`salt_and_pepper`/`phantom_obstacle` and `fgsm_attack` are registered/implemented but **unused offline** (only reachable via the live path or manual calls) | Document the active-vs-unused split; consider pruning or labelling the extras as live-only |
| 6.3 | `dataset.py:298-312`, `run_analysis.py:735-748`, `compute_test_chunk.py:165-217` | med | TFV6 deferred-PGD stores **clean pixels + label=1**; locally recomputed ATOMs profiles for these frames are silently non-adversarial unless HPC profiles are merged. Guarded by warnings only, not enforced | Persist a per-frame "pixels_deferred" flag and hard-error (not warn) if profiles are recomputed locally without merge |
| 6.4 | `atoms_config.py:84` (PGD_EPSILON=14) vs `:79` (EPSILON=8, comment "TF: 12") | med | Three distinct PGD ℓ∞ budgets coexist: offline label ε=14.0, live attack ε=8.0, config comment claims 12. Online and offline PGD are not the same attack strength | Single ε source; pass it explicitly to both live and offline paths |
| 6.5 | `prep_test_wor.py:27-32` vs `run_analysis.py:704-710` | med | WoR HPC prep uses a **4-way 25 % mix with no PGD**, while WoR local spec is a **5-way 20 % mix with PGD**. A WoR labeled set's composition (entry set and fractions) depends on whether it was built locally or on HPC — breaks the local↔HPC parity that the TFV6 path relies on | Align WoR HPC `_SPEC` with the 5-way spec, deferring WoR PGD to `compute_test_chunk.py` (as TFV6 does) |
| 6.6 | `perturbation_manager.py:750` (`np.random.normal`), `:1002-1013` (salt/pepper) | low/med | `gaussian_noise` and `salt_and_pepper` use the **global NumPy RNG**, not the labeling seed. Frame assignment is reproducible (seed 42) but the noise realisation is not — two seed-identical runs produce different perturbed pixels (non-deterministic non-PGD OOD signal) | Pass a seeded `np.random.Generator` into the perturbation functions, or seed the global RNG from `conf.RANDOM_SEED` before applying |
| 6.7 | `perturbation_manager.py:1051` (docstring "pixel value 10") vs `:1077` (`arr[...] = 20`) | low | `phantom_obstacle` docstring says the box is filled with value 10; the code writes 20 (deliberately, to avoid aliasing with camera_loss). Docstring stale | Update docstring to 20 |
| 6.8 | `sensor_agent_live_perturbation.py:137` (`nets=...nets`) vs `compute_test_chunk.py:210-211` (`nets=[tfv6_model]`) | med | Live PGD averages the gradient over the full 3-model ensemble; HPC offline PGD targets a **single** member. The analyzed adversarial perturbation is crafted against a different policy than the one that drove the live recordings (relevance facet of 2.5) | Either attack the same model set in both paths, or state the single-member offline attack explicitly in the thesis |

## From Topic 7 — Distances & Detectors

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 7.1 | `atoms_config.py:21,89` + `run_analysis.py:456,458,475-480` | low/med | `NUM_GMM_CLUSTERS = 10` (now equal to `GMM_MAX_K = 10`, so finding 1.10's "12>10" is resolved) still **always overrides** the BIC/AIC sweep result — the sweep is computed and plotted but never selects the live K unless config is `None`. Forced K=10 sits at the very top of the sweep range (max components). Verified precedence CLI `--gmm-k` > config > BIC matches CLAUDE.md | Set `NUM_GMM_CLUSTERS=None` to actually use BIC, or document that K is fixed at the sweep cap |
| 7.2 | `detectors.py:160,163` + `distance_computer.py:32` vs `atoms_config.py:88` | low | Three conflicting Mahalanobis ridge defaults coexist: docstring "1e-6 default; increase to 1e-4" (`detectors.py:160`), constructor default `1e-6` (`:163`), `compute_mahalanobis` default `0.01` (`distance_computer.py:32`). Effective value everywhere is `conf.MAHAL_RIDGE=0.01` (always passed explicitly); the constructor default and docstring are dead/misleading. Extends 1.14 | Align defaults to 0.01; fix docstring |
| 7.3 | `distance_computer.py:222,230-232,358-362` + signed TFV6 profiles (4.5) | med | `compute_jsd` renormalises a possibly mixed-sign profile by its (signed) sum and clips to ≥1e-12 — mathematically dubious when the profile sum is near zero or negatives are large; `compute_wasserstein` raises on `min<−1e-5`. JSD/GMM-JSD assume both vectors lie on a shared object simplex, which holds only loosely for renormalised and not at all for signed profiles. Extends 4.5 | Decide an abs/pos-clip policy for signed TFV6 profiles before JSD/W; assert non-negativity or guard near-zero sums |
| 7.4 | `detectors.py:524-584,591-662,669-793` vs `run_analysis.py:1136-1146,1148-1154` | med | `KNNDetector` (mean-of-k, unnormalised), `JensenShannonDetector` (sliced KDE JSD), `WassersteinDetector` (sliced W) are **never instantiated** in the offline pipeline — it uses `DistanceComputer.compute_knn_distance` (**k-th** neighbour, L2-normalised), `compute_jsd` (discrete-distribution JSD vs baseline mean), and disabled Wasserstein. The detector classes compute **different scores** than what is reported. Reproducibility/maintenance hazard | Either route the pipeline through the detector classes or delete the unused class score logic / document the divergence |
| 7.5 | `clustering.py:122-123,312` | low | `ridge_mat = self.ridge * np.eye(C)` computed in `fit` (and `load`) but never applied — the ridge is instead applied at score time inside `compute_mahalanobis`. Dead code | Remove the unused `ridge_mat`, or apply it to the stored covariances at fit |
| 7.6 | `atoms_config.py:45` (`MDX2_USE_FC_FEATURES=False`) vs `CLAUDE.md` (MDX-v2 = "256-d F_c") + `run_analysis.py:397-404` | med | CLAUDE.md states MDX-v2 uses 256-d F_c `speed_query`; the live config sets `MDX2_USE_FC_FEATURES=False`, so MDX-v2 actually uses the **512-d backbone** (same as v1) and the steer proxy stays 0.0. As configured, MDX-v2 differs from MDX-v1 only in `bin_strategy` (quantile vs equal-width), not feature space | Set the flag True to realise the documented F_c ablation, or correct CLAUDE.md to say the F_c half is currently disabled |
| 7.7 | `run_analysis.py:1350-1371,1387,1552-1556` | med | MDX-v2 is **fitted and saved** (`:412-415`) but its entire test-scoring block, sanity-check entry, and evaluation are commented out — MDX-v2 produces **no AUC** in the current pipeline. Only MDX-v1 contributes a number | Re-enable MDX-v2 scoring/eval, or drop MDX-v2 from the thesis detector list |
| 7.8 | `run_analysis.py:1491-1493,1522-1525` | low/med | k-NN and GMM-k-NN k selection falls back to **test-AUC argmax** (leakage) with only a `WARNING: k selected on test` print when no val set is present — not enforced. A run without val files silently tunes k on the test labels | Hard-error (not warn) when val is absent and k must be selected, or fix k to a config value |
| 7.9 | `run_analysis.py:1376-1378` + `papers/uncertainty based müller sedlmeier.pdf` Eq.2-3 | low | TFV6 "PEOC" substitutes the **8-bin speed distribution** for the policy `π(a|s)` of Sedlmeier et al. — it measures speed-prediction confidence only (no steering/waypoint uncertainty). Defensible adaptation but a deviation from the paper that is not documented in `docs/design_decisions.md` | Record the speed-distribution-as-proxy-policy choice as a design decision |
| 7.10 | `detectors.py:1036-1040` + `papers/Zhang ...` Eq.8 | low | Zhang's conformal calibration is **per-action-class** (`Q^c_{1−α}`); `MDXDetector.fit` computes a **single global** quantile over all calibration scores. Harmless for the AUC numbers (conformal threshold unused for ROC) but deviates from the paper's distribution-free guarantee | Compute per-class conformal quantiles, or note the simplification (it is unused for scoring) |
| 7.11 | `distance_computer.py:58-63,84-85,132-133,247-248,377-378` | low | All metrics silently clamp non-finite results to `1e6` (or JSD to `0.0`). A degenerate covariance, empty cluster pool, or zero-sum profile yields a finite-but-meaningless score instead of an error, which can quietly bias AUC | Log/raise on clamped values; surface degenerate-input warnings |

## From Topic 8 — Offline Analysis

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 8.1 | `run_analysis.py` STEP banners (`:126,247,423,448,485,513,670,727,905,1109,1408,1593,1701`) vs `CLAUDE.md` pipeline table | med | The in-code `STEP N` banner numbering does **not** match `CLAUDE.md`'s table: banner 3 = table 2.5 (MDX-v1); banners 4–9 are +1 vs the table (banner 4=table 3, …, banner 9=table 8); banner 10 = table 8.5 (dead); banner 11 = table 9 (scoring); banner 12 spans table 9.5+10+12; banner 13 = table 11; banner 14 = table 12. The module docstring's "Steps 1–12" (`:14-27`) is a fourth, idealised numbering, and sub-banners still say 9a–9e / 12a–12g | Renumber the code banners to one scheme and align `CLAUDE.md` + the docstring; see `documentation/08_offline_analysis.md` §3.1 for the reconciliation table |
| 8.2 | `run_analysis.py:872-902` (STEP 9.5) vs `:1109-1405` (STEP 11) | low/med | True control flow loads val (STEP 9.5) **before** any detector scoring (STEP 11), with the dead trajectory block (STEP 10) between them. `CLAUDE.md`'s table implies scoring (step 9) precedes val (step 9.5). Scoring consumes `_has_val` to build val score arrays inline (`:1210-1243,1275-1306`) | Correct `CLAUDE.md`'s step ordering to: 8 test → 9.5 val → 9 scoring (val before scoring) |
| 8.3 | `run_analysis.py:905-1106` | low | STEP 10 trajectory analysis is entirely commented out (confirms `CLAUDE.md`'s "DISABLED" claim), but the orphaned `print("[Step 10f] Generating figures...")` at `:1072` still executes every run — a misleading progress line for a step that writes nothing. No `trajectory_*.png` / `displacement_*` is produced; any such figures on disk are stale | Delete the dead block including the stray print, or restore the step |
| 8.4 | `run_analysis.py:1531-1544` + `CLAUDE.md` step 9.5 | low/med | `__val_auc_gmm_avg__` and `CLAUDE.md` claim a mean over **five** GMM detector variants, but Wasserstein-GMM is commented out (`:1538`), so the live mean is over **four** (Mahalanobis/Euclidean/JSD/k-NN GMM). `summarize_results.py` prose ("all five GMM detectors", `:399,415`) is also wrong | Update CLAUDE.md + summarize_results.py prose to "four GMM detectors" or re-enable Wasserstein-GMM |
| 8.5 | `run_analysis.py:493` vs `clustering.py` `select_n_components` seed | low | The fitted GMM at the chosen K uses `random_state=conf.RANDOM_SEED=17`; the BIC/AIC model-selection GMMs use hardcoded `random_state=42`. Model-selection and deployed GMM are different fits even at identical K | Use one seed source for both GMM fits |
| 8.6 | `run_analysis.py:110` + `visualization_carla.py:1005-1027` vs `CLAUDE.md` Data Layout | low | Real output dir is `atoms_analysis_mode_<mode>/` (mode-suffixed), not `atoms_analysis/`; `make_output_dirs` creates only `{pca,tsne,roc,attention,scores,clustering}/` — never the `trajectory_analysis/` subdir CLAUDE.md lists (it was created only by the now-dead STEP 10) | Update CLAUDE.md Data Layout: mode-suffixed dir name; drop `trajectory_analysis/` |
| 8.7 | `sweep_clusters.py:33 vs :42` | low | `K_VALUES_DEFAULT = [10, 8, 12]` but the `--k-values` help text says "default: 2 4 6 8 … 20". Supersedes finding 1.5 (which recorded the literal as `[18,20,22]` — the literal has since changed but the help string was never synced) | Sync the help string to the actual default |

## From Topic 9 — Online Analysis

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 9.1 | `run_online_analysis.py:1-30` | low | Module docstring is a **verbatim copy of `run_analysis.py`'s** — title says `run_analysis.py`, lists an offline "Steps 1–12" ROC/AUC/Youden outline this script never runs (it produces distance-over-time figures, no AUC). Extends/restates finding 1.11 | Rewrite the header to describe the live distance-over-time pipeline |
| 9.2 | `run_online_analysis.py:106-343` vs `run_analysis.py` baseline half + `:526-554` | low/med | Baseline-fitting STEPS 1–5 are **copy-pasted** from `run_analysis.py`, not imported, and omit the offline fixes: no MDX-v2, no baseline viz, no `--gmm-k` sweep, and **no `(run_id, frame_idx)` alignment-key guard** in the profile load branch (only a row-count check). The two will drift; mis-aligned profiles can pass silently if counts match | Factor the shared baseline-fit into a reusable function; add the alignment-key guard to the online load branch |
| 9.3 | `atoms_config.py:63,67,69` vs on-disk `data/TFV6/test_data{,_alt}/live_pert_frames/` + `results{,_alt}/live_perturbation/` | med | With `EXPERIMENT_VARIANT="alternative"`, `run_online_analysis.py` globs `test_data_alt/live_pert_frames/` which is **empty** → `FileNotFoundError` (`:379-383`); the actual live recordings/profiles/figures all live under the **original** `test_data/` and `results/live_perturbation/`. The online experiment is only runnable after flipping the flag to `"original"`. Live variant of finding 5.7 | Note the required variant for the live experiment; or migrate the live data under `_alt`; or decouple the live-pert path from `EXPERIMENT_VARIANT` |
| 9.4 | `run_online_analysis.py:93,95,376` + on-disk `attention/live_pert/brightness_scale/`, `test_data/{live_pert_frames_backup,alternative live_perts}/` | low/med | The single global `LIVE_PERT_NAME=conf.PERTURBATION` keys both the glob and the output folder, so re-running with a changed `PERTURBATION` leaves **stale cross-keyed profiles**: `attention/live_pert/brightness_scale/` contains profiles named after PGD variants (`brake_205328_000`, `nocrash_155706_000`, …) that the brightness glob never matches. Messy sibling dirs (`live_pert_frames_backup/` with `.bak`/`.bak2`; `alternative live_perts/` — **name contains a space** — holding ε/step-ablation recordings) accumulate uncleaned | Add a per-file `perturbation` field; clean/rename the stale folders; avoid spaces in dir names |
| 9.5 | `run_online_analysis.py:559-685` + `visualization_carla.py:1359-1414` | low/med | **No score smoothing/windowing of any kind** — per-frame distances are plotted raw with `"o-"` markers (verified: grep for rolling/moving-average/smooth/window/convolve/ema/savgol returns nothing). The traces are jittery, making the injection change-point hard to read by eye | Add an optional rolling-mean overlay (e.g. centred window) to `plot_distance_over_time` |
| 9.6 | `run_online_analysis.py:276-281,677-685` | med | Online experiment is **qualitative-only**: no ROC/AUC/Youden, no detector ranking; the 99th-pct Mahalanobis threshold is fitted and saved but **never applied** to flag frames (plots show raw scores, no alarm line). `summarize_results.py` Section 7 inventories the figures with no number | State explicitly the online result is illustrative; optionally draw the saved threshold as a horizontal line and report a per-drive alarm rate |
| 9.7 | `run_online_analysis.py:202` | low | WoR MDX-baseline config path is a **hardcoded absolute Windows literal** (`C:/Users/paulk/...`), unlike STEP 1's relative paths — non-portable | Use the same relative `WOR_WEIGHTS_DIR / "config_leaderboard.yaml"` as STEP 1 |
| 9.8 | `run_online_analysis.py:577-585,626-631` vs offline roster (Topic 7/8) | low/med | Online detector roster is a **reduced subset**: single Mahalanobis/Euclidean/JSD + **k-NN at a hardcoded k=25** + Mahalanobis-GMM-nearest + PEOC + MDX-v1. It omits Euclidean-GMM, JSD-GMM, k-NN-GMM, Wasserstein, the k-sweep, and MDX-v2. The k=25 is fixed with no val-AUC selection (offline selects k on val, Topic 7 §2.7) | Align the online roster with offline, or document the subset; select k on val or fix it in config |
| 9.9 | `run_online_analysis.py:668` | low | `_CARLA_HZ = 20` is **hardcoded inline** in the injection-frame fallback (`INJECTION_TIME * _CARLA_HZ`), with no config source. Harmless because the `argmax(is_perturbed)` path is always taken on recorded files, but a silent magic constant | Move the CARLA tick rate to config |
| 9.10 | `run_online_analysis.py:660-661,677-685` | low | `live_pert_dir = RESULTS_DIR/live_perturbation/<pert>` is created every iteration but **never used** — figures are saved to `OUT_DIR` instead. Dead directory creation (and explains the empty/stale `results/live_perturbation/<pert>/` dirs on disk) | Either save figures to `live_pert_dir` or remove its creation |
| 9.11 | `hpc/gather_live_pert.py:77,93` vs `run_online_analysis.py:412` + on-disk `attention/live_pert/pgd/` | med | HPC ↔ local **profile-naming mismatch**: `gather_live_pert.py` writes `live_pert_profiles.npy` (no variant, no mode suffix, **all variants concatenated**), but the local loader expects `live_pert_profiles_{variant}_{mode}.npy` **per variant**. The gathered HPC file is not picked up without a manual per-variant rename/split; both schemes coexist on disk (`live_pert_profiles_1.npy` AND `live_pert_profiles_brake_205328_000_1.npy`). The concat granularity (all variants pooled) is incompatible with the per-variant local analysis | Make `gather_live_pert.py` emit per-variant, mode-suffixed files matching the local loader; or make the loader accept the pooled file with a per-variant index |

## From Topic 10 — HPC Pipeline

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 10.1 | `hpc/requirements_hpc.txt:1,14-15` + all `hpc/array*_task.sh` (`#SBATCH` block) | med | **The Viper cluster is CPU-only, not GPU.** `requirements_hpc.txt` is titled "ATOMs baseline computation on MPCDF Viper-CPU" and pins CPU torch wheels (`torch==2.6.0+cpu`); **no SLURM script requests `--gpus`** (every array task is `--cpus-per-task=4`, no GPU). The compute workers' `cuda if available else cpu` branch therefore always runs on CPU; the 24 h array wall-times reflect CPU LRP cost. `cluster_explanations.md` and the plan frame the array stage as "GPU" | Drop the GPU framing in docs; if GPU is intended, add `--gpus` + a GPU partition and CUDA wheels |
| 10.2 | `hpc/requirements_hpc.txt:19` | low/med | `zennit` is **unpinned** in the HPC requirements, although the installed version determines the canonizer/composite semantics relied on by findings 3.3/3.4/3.10. The HPC env and local env can resolve different zennit versions → subtly different attributions | Pin `zennit==0.5.x` in both `requirements_hpc.txt` and `environment.yml` (3.10) |
| 10.3 | `hpc/requirements_hpc.txt:2,8,12` + `hpc/__pycache__/*.cpython-310.pyc` + CLAUDE.md (Python 3.10) | low | Python-version inconsistency: the requirements header says "Tested on Python 3.13" (line 2) and "torch 2.5.0 is the earliest build for Py3.13" (line 8), but line 12 pins **torch 2.6.0**, and the repo `__pycache__` is `cpython-310` matching CLAUDE.md's stated Python 3.10. HPC venv (3.13) ≠ local env (3.10); the header contradicts itself (2.5.0 "earliest" vs 2.6.0 pinned) | Fix the header to the actually-installed Python/torch; note HPC vs local interpreter difference |
| 10.4 | `hpc/gather_live_pert.py:90-101` vs `hpc/collect_results.sh:161-194` vs `run_online_analysis.py:412` | med | Two reconciliation paths for the live-pert naming mismatch (9.11) disagree: `collect_results.sh live_pert` **renames** each per-variant `live_pert_profiles_<mode>.npy` to `live_pert_profiles_{VARIANT}_{mode}.npy` (correct for the loader), but `gather_live_pert.py`'s own printed copy instructions copy the variant-less `live_pert_profiles.npy`, which the loader will not pick up. An operator following the gather script's printout stages an un-loadable file | Make `gather_live_pert.py`'s printed instructions match `collect_results.sh` (per-variant rename), or have gather embed the variant in the filename |
| 10.5 | `docs/cluster_explanations.md:160-162,199` vs `hpc/compute_test_chunk.py:47,53`, `hpc/array_test_task.sh:54,56` | low/med | `cluster_explanations.md` documents the array PGD attack as `target=steer_right, ε=12, 10 steps`; actual defaults are `target=brake` (`compute_test_chunk.py:47`), **8 steps** (`:53`), and ε is read from the labeled file first (recorded 12 / array fallback 14, finding 1.1). Three stale claims. The guide also omits the CPU nature of the array stage (10.1) and the `visualize_perturb.py` step run by `gather_test_task.sh:48-59` | Sync the cluster guide's PGD parameters and add the missing steps |
| 10.6 | `hpc/visualize_perturb.py:35,71` + finding 2.3 | low | `visualize_perturb.py` (the QC figure run inside `gather_test_task.sh`) defaults `--n-cameras 6`, but TFV6 LEAD frames are **3-camera** 1152 px (finding 2.3). The "middle camera" index `n_cameras // 2 = 3` then crops a band that does not exist in a 3-cam strip, so the sanity figure shows the wrong slice | Default `--n-cameras` to 3 for TFV6, or read the camera count from the frame width |
| 10.7 | `docs/cluster_explanations.md` (throughout) + `hpc/compute_mdx_features.py:17-19,137` | low | Hardcoded usernames/paths in the operator docs: `cluster_explanations.md` hardcodes `paulkull` and absolute `/u/paulkull` / `C:\Users\paulk` paths in every example; `compute_mdx_features.py`'s docstring does the same. The SLURM scripts correctly use `$USER`, but the human-facing guide is non-portable | Use `$USER` / placeholder tokens in the docs |
| 10.8 | `hpc/compute_*_chunk.py` (no `import atoms_config`) vs `ATOMs_Analysis/atoms_config.py` | med | **No shared config between local and HPC.** The chunk workers never import `atoms_config.py`; every shared hyperparameter (p_relevance, default_cmd, mode_analysis, PGD ε/steps/target, seed) is duplicated as a source literal or SBATCH env default with no coupling or assertion. This is the structural root cause of findings 4.3 (0.25 vs 0.9), 1.1 (ε 12 vs 14), 6.5 (WoR 4-way vs 5-way) | Introduce a minimal config module importable in the stub env, or pass all such values as required CLI args sourced from `conf` at submit time |
| 10.9 | `hpc/prep_live_pert.py:83-84`, `hpc/prep_test.py:170-171` vs `hpc/submit_live_pert.sh:43`, `hpc/submit_test.sh:44-48` | low | `live_pert_meta.txt` and `test_meta.txt` are written "to size the SLURM array", but neither is read for sizing: `submit_live_pert.sh` hardcodes `MAX_FRAMES=200`; `submit_test.sh` re-counts frames from `test_labeled.npz` directly. The meta files are informational only — their stated purpose is unimplemented | Either size the array from the meta file, or correct the prep docstrings to "informational" |

## From Topic 11 — Validation & Testing

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 11.1 | `ATOMs_Analysis/detection/baseline_dataset.py:59,476-496` + repo-wide (no pytest/conftest/CI; Glob-confirmed) | med | **The entire LRP/ATOMs validation harness is dead-by-default.** Only the `LRPTestSuite` import (`:59`) is live (and unused); every instantiation + `run_all_tests`/`print_report` call is commented (`:477-495`). Repo-wide search for `run_all_tests`/`*TestSuite(`/`*Diagnostics(` finds only these commented lines + docstring examples + method defs — no external caller. No `pytest.ini`/`conftest.py`/`tox.ini`/`setup.cfg`/`pyproject.toml`/`.github/workflows`, no `RUN_TESTS` config flag, no aggregate exit code. Regressions in LRP/ATOMs are caught only if the author manually un-comments and runs the suite. Same "feature commented out / dead" pattern as 2.9, 7.7, 8.3, 8.4 | Add a `tests/` entry point or a `RUN_TESTS` flag that runs the agent-correct suite + diagnostics with a non-zero exit on FAIL; or at minimum a standalone `run_validation.py` and document the last-passing run |
| 11.2 | `atoms_test_suite.py:910-963` (A10), `:444-498` (A04), `:969-1041` (A11); `tfv6_test_suite.py:729-767` (A02) vs finding 4.5 | med | The ATOMs suite asserts **non-negative per-class contributions** (A10, TFV6-mirror A02) and **rows summing to ≈1 on a non-negative simplex** (A04/A09/A11). These hold for WoR (cross-normalize abs's) but are **violated by signed TFV6 profiles** (finding 4.5): raw signed maps yield negative class sums and a possibly-near-zero signed normalizing total. If A10/A02 were run on TFV6 they should FAIL; A04/A11 can fail or pass spuriously. Internally inconsistent with the same TFV6 suite's L04, which *accepts* signed maps via AttnLRP-adjusted thresholds | Decide an abs/pos-clip policy for TFV6 maps (consistent with the 4.5 fix); make A02/A10 agent-aware (assert non-neg only after the chosen abs); reconcile L04 vs A02 |
| 11.3 | `baseline_dataset.py:56,434,482-491` + `lrp_test_suite.py:141-146`, `atoms_test_suite.py:69,1128`, `tfv6_test_suite.py` | low/med | Suites are agent-specific but the binding is **unenforced**, and the dead block (11.1) constructs the **TFV6** suite/diagnostics against `BaselineComputer.self.lrp`, which is a **WoR** `LRPCameraModel` (`:56,434`) → would ERROR if un-commented. `LRPTestSuite`/`ATOMsTestSuite` are WoR-only (`num_steers`, `_build_drive_brake_selector`, `_model_eval.policy`, `CARLA_CLASSES`, mandatory `narr`); `TFV6TestSuite`/`TFV6LRPDiagnostics` are TFV6-only. No agent-type assertion in any suite ctor | Add an agent-type assert at each suite/diagnostic ctor; in the test block branch on `conf.AGENT` to pick the correct harness |
| 11.4 | `lrp_test_suite.py:909-979` (T12) reads `self.lrp.act_fc_model_lrp.head.fixed_context` | low/med | **T12 is stale and can never pass.** Neither `act_fc_model_lrp` nor `fixed_context` exists on the live `LRPCameraModel` (grep-confirmed; class exposes `fc_model_lrp`/`model_lrp`, no frozen-context buffer). The `AttributeError` is swallowed → T12 always returns WARN ("Could not access fixed_context buffer"). The determinism + frame-sensitivity property it claims to test is **never actually exercised** for WoR | Repoint T12 to the real context mechanism (or use W08-style repeat-call determinism) and remove the stale attribute path |
| 11.5 | `lrp_test_suite.py:197` (`print(exec)`), `:1325` ("relevbance" in T16 FAIL msg) | low | Never-exercised error/FAIL paths contain bugs: `print(exec)` prints the builtin instead of the traceback `exc` when a test raises; T16's conservation-FAIL message misspells "relevance". Both are in branches that have evidently never run — circumstantial confirmation the suite is not routinely executed (supports 11.1) | Fix `exc`; fix typo; better, exercise the paths once |
| 11.6 | `lrp_test_suite.py`, `tfv6_test_suite.py`, `atoms_test_suite.py`, `tfv6_lrp_diagnostics.py`, `wor_lrp_diagnostics.py` (all) | low | `TestResult`/`_safe_run`/`_to_tensor`/`_gini`/`_entropy_bits` and the `print_report`/`save_report` machinery are duplicated **verbatim across all five harnesses** (the TFV6 suite even comments "mirrored from lrp_test_suite.py"). ~300 lines of drift-prone duplication; helpers already diverge slightly (e.g. two `_gini` formulas) | Factor a shared `test_common.py` (dataclass + helpers + reporting) imported by all five |

## From Topic 12 — Visualization

| # | Location | Sev | Finding | Suggested fix |
|---|----------|-----|---------|---------------|
| 12.1 | `visualization_carla.py:57-104` vs `atoms_carla.py:57-103` (restates 4.9) | low | **Confirmed**: `CARLA_CLASSES` (29) and `TFV6_CLASSES` (10) are defined a second time in the viz module, identical to the `atoms_carla` copies but maintained independently. Local code imports the class map from `atoms_carla`, HPC scripts from `visualization_carla` → silent divergence risk for legend/profile semantics. The viz docstring (`:91-92`) also says LEAD collapses "32 raw CARLA classes" while `CARLA_CLASSES` lists 29 (tag-count drift, cf. 1.12, 4.1) | Single source of truth: define once, re-export from the other module |
| 12.2 | `viz_config.py:91-92` + `visualization_carla.py:416,459,485` | low | Cluster colormap caps at 20 distinct colours (`CLUSTER_CMAP_LARGE="tab20"`). With K>20 (allowed via `--gmm-k`) cluster colours wrap and become indistinguishable in `plot_pca_clusters`/`plot_tsne_clusters`/`get_cluster_colors`; no guard or warning. Latent only because current sweeps stay ≤12 | Use a continuous colormap (e.g. `hsv`/`viridis`) for K>20, or assert K≤20 |
| 12.3 | `visualization_carla.py:1083-1356` (whole trajectory family) + `run_analysis.py:1057-1104` (commented) (restates 8.3) | low/med | `compute_perturbation_displacement_stats`, `format_displacement_stats_text`, `plot_pca_perturbation_trajectories`, `plot_displacement_coherence_bar`, `plot_displacement_magnitude_boxplot` are fully implemented but **dead** — their only callers are the commented-out STEP 10 block. No `trajectory_*`/`displacement_*` figure is regenerated; any on disk is stale. `fit_whitened_pca`/`apply_whitened_pca` are reachable only via this dead family | Re-enable STEP 10, or remove the dead functions and note the figures are not produced |
| 12.4 | `visualization_carla.py:107,149,192,1359` vs module docstring `:28-29` | low | Four functions violate the "return a Figure, caller saves" contract: `visualize_relevance`, `visualize_comparative_relevance`, `visualize_segmentation` save internally and return `None`; `plot_distance_over_time` builds, **names**, and saves its own file. Mixed conventions complicate reuse/testing | Either make all builders return a Figure, or document the two intentional save-internally families |
| 12.5 | `viz_config.py:155,193-201,55-71` | low | Unused-in-CARLA constants kept only for Atari byte-parity: `FIGSIZE_BAR` (never read — bars use the scaling helpers), the `TRAJ_POINT_SIZE`/`TRAJ_LINE_WIDTH`/`TRAJ_LINE_ALPHA`/`TRAJ_ARROW_LINEWIDTH`/`TRAJ_START_MARKER`/`TRAJ_END_MARKER`/`TRAJ_ENDPOINT_SIZE` block, `SAVE_FORMAT_DEFAULT` (never consulted), and ~8 `PERTURBATION_COLORS` keys (`blur`, `salt_*`, `*_brightness`, `random_occlusion`, `phantom_obstacle`). A reader cannot distinguish live constants from cross-project ballast | Comment-mark Atari-only constants, or split a `viz_config_shared` vs `viz_config_carla` |
| 12.6 | `visualization_carla.py:276` (`fit_tsne`) | low | t-SNE fixes only `random_state=42`; `perplexity`/`n_iter` are never set → sklearn defaults that depend on N and sklearn version. t-SNE figures are deterministic for a fixed env but not reproducible across sklearn versions, and perplexity is not tuned to the baseline cloud size | Set `perplexity`/`max_iter` explicitly (and pin sklearn) for reproducibility |
| 12.7 | `run_online_analysis.py:441,447,512,517` → `visualize_relevance` `save_path` | low | Online relevance overlays are saved to extension-less paths (`relevance_wide_<i>`, no `.png`). `plt.savefig` infers PNG from rcParams but the files land without an extension, so they don't open by double-click and look like non-images on disk | Append `.png` to the `save_path` (or have `visualize_relevance` add the default extension) |
| 12.8 | `visualization_carla.py:134-140,171-182` vs docstrings `:157,156` | low | `visualize_relevance` docstring/`FIGSIZE_ATTENTION_OVERLAY=(15,4)` imply a 3-panel Input\|Relevance\|Overlay triptych, but only a 1×2 Relevance\|Overlay is drawn (input RGB is folded into the overlay). `visualize_comparative_relevance` docstring likewise claims "Input \| Drive − Brake \| Overlay" but builds 1×2 | Correct the docstrings to the actual 2-panel layout, or add the input panel |
| 12.9 | `visualization_carla.py:1416-1441` vs `:1444-1472` | low | `_distance_type_from_detector_name` and `_get_plot_style` hardcode the **same** most-specific-first metric-matching ladder twice (one returns a key-or-None, the other a `(color, ylabel)` tuple). An edit to the colour keys must be mirrored in both or they drift | Factor a single `_distance_key(name) -> Optional[str]` and have both helpers call it |
